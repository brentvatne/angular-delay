describe('angular-delay', function () {
  beforeEach(module('ngDelay'));

  var $scope;

  beforeEach(inject(function ($rootScope) {
    $scope = $rootScope.$new();
  }));

  // happy path(s)
  it('', function () {
  });

  // null input path
  it('', function () {
  });

  // wrong type path
  it('', function () {
  });

});
